id: 001-no-file-selected
preset: image/compress
source:
  type: synthetic
  citation: "Mirrors engines/image image.resize@1 fixture 001-no-file-selected: a missing file must be a specific, typed error through the Image Compress preset too"
input:
  targetWidth: 100
  targetHeight: 100
  keepAspectRatio: true
  outputFormat: same
expectedError:
  code: IMAGE_NO_FILE_SELECTED
