# yaml-language-server: $schema=../../generated/schemas/preset.json
# Foundation-only (TASK-005B PR 1): exists so the browser worker can load engines/image
# (generated/engine-loaders.ts is built from presets, not manifests) for the dev-only image-foundation
# harness. No tool manifest references this preset yet, so it is not visible anywhere on the site — no
# route, no category exposure, no search entry. UI wiring (archetype fields, strings, outputs) is deferred
# to PR 2, once a real tool exists to design them against.
presetVersion: 1
id: image/resize
version: 0.1.0
operation: image.resize@1
ui:
  archetypes: [D]
