id: 003-invalid-dimensions
operation: image.resize@1
source:
  type: synthetic
  citation: "tiny.jpg (fixtures/files/) has a valid JPEG signature; this fixture checks that a zero target dimension is rejected before any attempt to decode the file"
input:
  file: { name: tiny.jpg, bytes: { path: tiny.jpg } }
  targetWidth: 0
  targetHeight: 100
  keepAspectRatio: true
  outputFormat: same
expectedError:
  code: IMAGE_DIMENSIONS_INVALID
